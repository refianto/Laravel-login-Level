# This laravel has modified 

# Usually i use laragon app to run this laravel

# I have set the database u just run command in CMD "php artisan migrate" or you can import file "mlog.sql" in phpmyadmin